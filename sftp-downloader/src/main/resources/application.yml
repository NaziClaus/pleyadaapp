sftp:
  host: ${SFTP_HOST}
  port: ${SFTP_PORT:22}
  username: ${SFTP_USERNAME}
  password: ${SFTP_PASSWORD}
  remote-dir: ${SFTP_REMOTE_DIR:/}
  # Path inside the container; host path is configured in docker-compose
  local-dir: ${SFTP_LOCAL_DIR:/app/download}
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/sftpdb}
    username: ${SPRING_DATASOURCE_USERNAME:sftpuser}
    password: ${SPRING_DATASOURCE_PASSWORD:sftppass}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    root: INFO
